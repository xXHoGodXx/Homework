let name = (`alex`)
let money =  (`10000`)
let account =  (`7777`)

let a = prompt(`Whats ur name?`)

if (a === `alex` ) {
   
  let a = prompt(`номер счета?`)
  if  (a ===`7777`) {
     let m = prompt(`Сколько обналичить?`)
    
    if (m===`10000`) {
     prompt (`Сколько снял сколько осталось?`)
    } else {
      alert (`Недостаточно средств`)
    }

  } else {
    alert (`Пользователь не найден, досвидули`
    )
  }

  } else  {
   alert(`Пользователь не найден, досвидули`)
}
   
    
 
 


  

 
  


    
